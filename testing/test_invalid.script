report 'Report Name'

description 'Report description'

template 'test.xlsx'

parameter Client string

CONNECTION pg 'pg.conn'

output 'asdf-{{.Client}}.xlsx'

query 'name1' from pg (
    SELECT 1
) into sheet 'summary' range [0,0]:[0,1]

QUERY 'name2' FROM pg (
SELECT 2
) into sheet range [0,0]:[0,n]